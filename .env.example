# Postgres connection string (Neon). Without it the simulation still runs —
# the aggregate panels simply report that they have no data.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
